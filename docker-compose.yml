# sudo apt install docker-compose
# sudo docker-compose up -d
# version: '3.8'
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /home/nietzchan/Documents/VvvebJs:/var/www/html       
    ports:
      - 8080:80
